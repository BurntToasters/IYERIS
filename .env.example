# GitHub token used for release publishing and upload automation.
GH_TOKEN=""

# GPG signing configuration for release artifacts.
GPG_KEY_ID=""
# Leave GPG_PASSPHRASE empty if key does not require one.
GPG_PASSPHRASE=""

# Apple notarization credentials.
APPLE_ID=""
APPLE_APP_SPECIFIC_PASSWORD=""
APPLE_TEAM_ID=""

# Optional destination to mirror cleaned release artifacts after release builds.
AFTER_PACK_LOC=""
